体力:
  name: "体力"
  # 默认值
  default: 160
  # 最大值，默认无限制
  max: 160
  # 最小值，默认无限制
  min: 0
  # 定时任务
  task:
    # 默认每五分钟恢复1点体力
    体力恢复:
      cron: "0 0/5 * * * ?"
      # n 是原数值变量
      value: "n+1"
    # 每天十二点重置体力
    体力重置:
      cron: "00 00 00 ? * 1"
      value: "160"