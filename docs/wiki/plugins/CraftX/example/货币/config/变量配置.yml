摩拉:
  name: "摩拉"
  default: 0
  # 转账条件，支持使用 YeeCore 表达式。 "true" 表示不限制
  pay-condition: true
成就点:
  name: "成就点"
  default: 0
绑定点券:
  default: 0
  # 绑定货币
  # PlayerPoints: point, Vault: money, CraftX: 配置变量Id
  bind: "points"
  pay-condition: true
绑定金币:
  default: 0
  bind: "money"
  pay-condition: true
绑定摩拉:
  default: 0
  bind: "摩拉"
  pay-condition: true
金币:
  # 经济桥
  # PlayerPoints: point, Vault: money, CraftX: 配置变量Id
  bridge: "money"
点券:
  bridge: "points"
  pay-condition: true